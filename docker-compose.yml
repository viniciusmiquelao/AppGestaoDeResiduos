version: "3.8"

services:
  app:
    build: .
    container_name: gestao_residuos_app
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    networks:
      - gestao_net

networks:
  gestao_net:
    driver: bridge
